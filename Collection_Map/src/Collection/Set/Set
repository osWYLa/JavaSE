Set 接口
    1）无序（添加和取出的顺序不一致），没有索引；（取出的顺序并不是添加的顺序）
    2）不允许重复元素，最多包含一个null;
    3)JDK API中，Set接口的实现类有：

Set接口常用方法：
    Set接口为Collection接口的子接口，因此常用方法和Collection接口一样；
Set接口的遍历方式：
    同Collection的遍历方式（Set是Collection接口的子接口）
    1、可以使用迭代器；
    2、增强for；
    3、不可以使用索引的方式获取；