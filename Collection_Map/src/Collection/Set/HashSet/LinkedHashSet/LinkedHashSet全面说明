LinkedHashSet
    1）HashSet的子类；
    2）LinkedHashSet根据元素的hashCode值来决定元素的存储位置，但它同时使用链表维护元素的次序；使得元素看起来是以插入顺序保存的；
    3）LinkedHashSet 添加性能略低于HashSet，迭代访问Set里的全部元素时有很好的性能。（底层维护了一个hash表+双向链表）
    4）LinkedHashSet不允许集合元素重复；（需要实现）
    5）LinkedHashSet底层是一个LinkedHashMap，维护的链表是一个双向链表。
