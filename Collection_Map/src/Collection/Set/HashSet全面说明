HashSet
    实现了Set接口;
    HashSet底层实际上是HashMap;
    可以存放null，但是只能有一个null;
    HashSet不保证元素是有序的，取决于hash后，在进行索引的结果；
    不能有重复元素，同前面的Set接口

    HashSet只是提供关于重复对象的一套机制，具体实现起来还是看有没有实现hashCode方法和wquals方法。
        没有重写依然走Object的默认方法，达不到效果。必须要重写，再动态绑定；
    类似于Arrays.sort中，始终需要动态绑定当自己实现的过程中；

    Set进行删除，添加时还是比较快的

