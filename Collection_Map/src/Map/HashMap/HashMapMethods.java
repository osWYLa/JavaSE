package Map.HashMap;

import java.util.HashMap;
import java.util.Map;

/**
 * @author wyl
 * @create 2020-09-08
 * @Description
 * @Version
 */
public class HashMapMethods {
    public static void main(String[] args) {
        Map hashMap = new HashMap();
        //Map接口的常用方法
        //put 添加(如果key存在则会替换key对应的value的值；key 可以为空，但只能有一个，值可以有多个空；key是唯一的；
        //        key-value 取出的顺序和添加的顺序是不同的（但确定之后每次输出都是一致的）)

        //remove 根据键删除映射关系； （若没有找到这个key也不会报错，不删除）

        //get 根据键获取值（若没有找到这个key也不会报错，不获取）

        //size 获取元素个数（键值对个数）

        //isEmpty 判断元素个数是否为0

        //clear 清除

        //containKey 查找是否存在 某键
    }
}
